package main

var a [10]int
var b int
func fib(x int) (int){
     if ((x==0) || (x==1)){
         return 1
     }
     return fib(x-1)+fib(x-2)
 }
 func main(){ 
     y:=5
     x:=fib(7)
     for i:=0;i<10;i++ {
         if True {
             if True {
                 if True {
                    a[i] = x
                 }
             }
         }
         a[i] = x
         j:=0
     }
     for i:=0;i<10;i++ {
         print a[i]
     }
     b=2
     print b+9
 }

